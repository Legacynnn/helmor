# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `2818226c`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Replace `file-extension-icon-js` with a dependency-clean icon source | P1 | S | — | TODO |
| 002  | Enable CSP and scope the Tauri asset protocol | P1 | M | — | TODO |
| 003  | Unit-test the sidecar request parser | P1 | M | — | TODO |
| 004  | Characterization tests for `workspace/branching.rs` | P2 | M | — | TODO |
| 005  | Remove Storybook | P2 | S | — | TODO |
| 006  | Split the 5,002-line `src/lib/api.ts` into domain modules | P2 | L | — | TODO |
| 007  | Extract shared infrastructure from the sidecar session managers | P3 | L | 003 | TODO |
| 008  | Spike: terminal sessions via CLI/MCP | P3 | M | — | TODO |
| 009  | README: surface Conductor import + CLI/MCP | P3 | S | — | TODO |
| 010  | Pipeline & storage ADR | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 007 requires 003: the request-parser tests are the characterization net for
  the sidecar wire contract before any manager consolidation.
- 001 + 005 together eliminate most current `bun audit` noise — re-run
  `bun audit` after both land before acting on remaining advisories.
- 006 (api.ts split) should land in a quiet window — api.ts is top-2 churn;
  coordinate with in-flight branches.

## Findings tracked as issues only (no plan written)

Published to GitHub at the maintainer's request; pick up directly from the issue:

- E2E happy-path coverage (3 specs today; no create-workspace → send → stream test).
- `schema.rs:349-380` repo-dedup migration runs UPDATE+DELETE via
  `execute_batch` without a transaction (idempotent + re-runnable, so impact
  is low — hygiene fix).
- Oversized frontend files breaking the repo's <300-line rule:
  `local-llm.tsx` 1652, `composer/index.tsx` 1432, `lib/settings.ts` 1415,
  `navigation/index.tsx` 1364, `use-streaming.ts` 1213.
- Perf investigations: zero-width-regex iteration in
  `workspace/files/search.rs:244-251`; per-row `git branch --show-current`
  (`workspace/helpers.rs:83`, author's own TODO — profile first).

## Findings considered and rejected (do not re-audit)

- Sidecar `unwrap` panic after reader-thread failure (`sidecar.rs:461,467`):
  false — the failure path returns `Err` at line 450; the unwraps are
  unreachable with `guard == None`.
- "immer prototype pollution via zustand": false — zustand has no immer
  dependency; the tree's `immer@11.1.4` (via `@lobehub/ui`) is not vulnerable;
  the ancient `immer@8` enters only through `file-extension-icon-js` (plan 001).
- "Workspace search has no debounce": false — `use-workspace-search.ts:25`
  debounces at 300ms.
- "Per-token React Query invalidation storm": false — `SessionMessagesAppended`
  publishes from codex-goal/triage paths, not the streaming hot path;
  streaming uses a direct `tauri::ipc::Channel`.
- "git ops and watcher untested": false — `git/ops.rs` has 29 tests,
  `watcher.rs` has a test module. The real gap is `branching.rs` (plan 004).
- "No frontend build in CI": false — `build.yml:37` runs `bun run build` on PRs.
- focusManager listener race (`query-client.ts:202-219`): real in theory, but
  the listener is an app-lifetime singleton — not worth doing.
- `GH_TOKEN` passed via child-process env (`forge/github/accounts.rs`):
  correct pattern (better than argv); not a finding.
- bun-audit advisories on minimatch/semver/rollup/qs/webpack-dev-server:
  dev-time transitive noise, almost entirely from `file-extension-icon-js`
  and Storybook chains — addressed structurally by plans 001 and 005.
- Zustand → Context migration: stylistic; team's call, no defect.
- Streaming-viewport perf claims (ResizeObserver thrash, width-bucket
  re-estimates, indicator position race): MED-confidence speculation without
  profiling; use `bun run dev:analyze` (perf HUD) before acting.
