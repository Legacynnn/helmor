---
"helmor": patch
---

Clicking a file change in the conversation now opens its git diff in the editor (from the change row or the file name in the hover preview) instead of just expanding the tool row.
