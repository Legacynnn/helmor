---
"helmor": minor
---

Canvas visual customization (translucency, background pattern/color/theme/image, snap-to-grid) is now shared per repository instead of per workspace — editing it in one workspace of a repo instantly restyles every open workspace of that repo, while camera position and panels stay per workspace.
