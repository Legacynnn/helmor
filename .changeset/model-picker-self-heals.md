---
"helmor": patch
---

The composer's model picker now refreshes automatically when a provider's model list changes, so newly fetched models (e.g. a freshly released Cursor model) appear without reloading the app.
