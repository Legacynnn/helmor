---
"helmor": minor
---

The file editor now offers Tailwind CSS class completions inside `class=`/`className=` attributes and `@apply`, with color swatches. The catalog covers the default Tailwind utilities and palette and is augmented with custom colors/fonts read from the open workspace's Tailwind theme (`@theme` for v4, `tailwind.config.*` for v3).
