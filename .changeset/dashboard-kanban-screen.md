---
"helmor": minor
---

Add a Dashboard screen reachable from new Dashboard/Tasks/History nav buttons in the sidebar, leaving the workspace list untouched below them.

- The Dashboard shows a Kanban board of workspaces grouped into status columns (In progress, Review, Done, Backlog, Canceled) with a summary header and a live indicator on workspaces that are currently running.
- Drag a card between columns to change that workspace's status; the board and sidebar stay in sync.
