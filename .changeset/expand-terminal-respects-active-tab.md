---
"helmor": patch
---

The "Expand terminal" shortcut now defaults to Cmd+Shift+E and enlarges whichever inspector tab you're already on — the Run output or a terminal — instead of always switching to a terminal tab first.
